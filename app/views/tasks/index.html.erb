<h1>Tasks</h1>
<ul>
<% @tasks.each do |task| %>
  <li><%= check_box_tag "completed", "1", task.completed, disabled: true %><%= link_to task[:title], task_path(task.id) %></li>
<% end %>
<%= link_to "Create a new task", tasks_new_path %>
</ul>
